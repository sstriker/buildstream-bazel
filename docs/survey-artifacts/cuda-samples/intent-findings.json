```json
{"findings":[{"category":"flag","severity":"high","summary":"CUDA GPU architecture list not captured — binary will silently target only the default/native arch instead of the required set","evidence":"cmake sets CMAKE_CUDA_ARCHITECTURES 75 80 86 87 89 90 100 110 120; Bazel cuda_binary has no cuda_archs attribute, so nvcc uses its toolchain default (single native arch) and the required SM targets are silently dropped","cmake_ref":"CMakeLists.txt:11"},{"category":"flag","severity":"high","summary":"CUDA separable compilation (device-code RDC) not captured — device linking semantics differ silently","evidence":"cmake: set_target_properties(vectorAdd PROPERTIES CUDA_SEPARABLE_COMPILATION ON) which passes -dc to nvcc and enables separate device-link step; Bazel cuda_binary has no rdc = True attribute, so device code is compiled non-relocatably","cmake_ref":"CMakeLists.txt:30"},{"category":"option","severity":"medium","summary":"ENABLE_CUDA_DEBUG build option lost — the -G flag (cuda-gdb full debug info) is permanently unreachable","evidence":"cmake guards -G vs -lineinfo on ENABLE_CUDA_DEBUG (lines 13-17); Bazel BUILD hard-codes -lineinfo in copts with no select() path that emits -G, so the cuda-gdb instrumentation option is silently dropped","cmake_ref":"CMakeLists.txt:13"},{"category":"other","severity":"medium","summary":"../../../Common include-path not forwarded — any Common/ headers used by vectorAdd.cu will be silently missing at Bazel build time","evidence":"cmake: include_directories(../../../Common) adds the shared CUDA-samples helper-header tree (helper_cuda.h, helper_math.h, …) to the search path; Bazel cuda_binary has no includes, hdrs, or deps that surface the Common/ directory","cmake_ref":"CMakeLists.txt:20"}]}
```
