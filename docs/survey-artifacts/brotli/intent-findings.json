Now I have enough to complete the audit. Let me produce the findings.

```json
{"findings":[{"category":"install","severity":"high","summary":"pkg-config files generated and installed by CMake are entirely absent from the Bazel conversion — no genrule to expand the .pc.in templates and no pkg_files targets for any of the three .pc files","evidence":"CMake generates libbrotlicommon.pc, libbrotlidec.pc, libbrotlienc.pc from scripts/*.pc.in at build time (transform_pc_file) and installs them to ${CMAKE_INSTALL_LIBDIR}/pkgconfig. Neither BUILD.bazel (shared or static variant) contains any genrule or pkg_files target for these files; the only install targets present are for man pages.","cmake_ref":"CMakeLists.txt:370"},{"category":"flag","severity":"high","summary":"libm link dependency dropped: CMake detects and links -lm on platforms that need it; the Bazel libraries have no linkopts = [\"-lm\"]","evidence":"CMakeLists.txt calls CHECK_LIBRARY_EXISTS(m log2) and, when found, appends 'm' to target_link_libraries for all three libraries (brotlicommon, brotlidec, brotlienc) and to the BROTLI_LIBRARIES set used by the brotli binary. Neither BUILD.bazel (top-level shared variant nor elements/ static variant) adds linkopts = [\"-lm\"] to any cc_library or cc_binary target. On Linux with glibc the encoder uses log2 and will fail to link without -lm.","cmake_ref":"CMakeLists.txt:95"},{"category":"test","severity":"high","summary":"Compatibility test suite (run-compatibility-test.cmake) is silently dropped with no todo/rejection entry — only the roundtrip test has a flagged todo","evidence":"CMakeLists.txt scans tests/testdata/*.compressed* and for each file whose uncompressed counterpart exists creates a CTest test invoking run-compatibility-test.cmake (decompress, then file-equality check). There are no test targets of any kind in either BUILD.bazel. The already-flagged set covers only todo-bcffb1fa22eb881f for run-roundtrip-test.cmake; run-compatibility-test.cmake and its associated data-driven test matrix have no corresponding todo or rejection entry.","cmake_ref":"CMakeLists.txt:287"},{"category":"install","severity":"medium","summary":"brotli CLI binary install target missing: no pkg_files for the compiled executable","evidence":"CMakeLists.txt installs the brotli executable to ${CMAKE_INSTALL_BINDIR} when BROTLI_BUILD_TOOLS=ON (default). Neither BUILD.bazel contains a pkg_files target for the brotli cc_binary; the only install targets present are man pages.","cmake_ref":"CMakeLists.txt:187"},{"category":"install","severity":"medium","summary":"Public header install target missing: no pkg_files for c/include/brotli/*.h","evidence":"CMakeLists.txt installs the brotli/ subdirectory of BROTLI_INCLUDE_DIRS (i.e. c/include/brotli) to ${CMAKE_INSTALL_INCLUDEDIR}. The c_include_headers cc_library exposes these headers for in-build use, but neither BUILD.bazel contains a pkg_files target pointing at those headers for the install layout.","cmake_ref":"CMakeLists.txt:201"},{"category":"install","severity":"medium","summary":"Library archive/SO install targets missing: no pkg_files for brotlicommon, brotlidec, brotlienc","evidence":"CMakeLists.txt installs BROTLI_SHARED_LIBRARIES and BROTLI_STATIC_LIBRARIES with ARCHIVE, LIBRARY, and RUNTIME destination clauses. Neither BUILD.bazel adds pkg_files targets for any of the three cc_library outputs, leaving the library install layout unmapped.","cmake_ref":"CMakeLists.txt:193"},{"category":"option","severity":"medium","summary":"Default build type inverted: CMake defaults to Release when unset; Bazel config defaults to debug","evidence":"CMakeLists.txt lines 13-15 explicitly set CMAKE_BUILD_TYPE to 'Release' when no build type is specified. config/BUILD.bazel sets build_setting_default = \"debug\" for the string_flag, so an unset --//config:build_type flag produces a debug (unoptimized, no NDEBUG) build instead of an optimized release build.","cmake_ref":"CMakeLists.txt:15"},{"category":"flag","severity":"low","summary":"MSVC _CRT_SECURE_NO_WARNINGS preprocessor define not carried into Bazel Windows builds","evidence":"CMakeLists.txt adds -D_CRT_SECURE_NO_WARNINGS globally when MSVC is the compiler. Neither BUILD.bazel contains a select() branch for windows_msvc or any equivalent @platforms//os:windows constraint that would inject this define, so MSVC builds will emit security-related deprecation warnings (or errors with /WX).","cmake_ref":"CMakeLists.txt:125"}]}
```
